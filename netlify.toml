[build]
  base = "Frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA routing (same as public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
